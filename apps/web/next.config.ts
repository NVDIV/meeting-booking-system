import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // Place it directly at the root level of the configuration object
  allowedDevOrigins: ['192.168.56.1']
};

export default nextConfig;